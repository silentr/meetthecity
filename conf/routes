# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Home page

GET     /                           controllers.Application.index()
GET     /tours                      controllers.Application.tours()
GET     /signin                     controllers.Application.signin()
POST    /signin                     controllers.Application.authenticate()
GET     /user                       controllers.Application.addUser()
POST    /user                       controllers.Application.createUser()
GET     /user/all                   controllers.Application.getAllUsers()
GET     /user/:id                   controllers.Application.getUser(id : String)
GET     /join-a-tour/:id            controllers.Application.joinATour(id: String)
GET     /reviews                    controllers.Application.getAllReviews()
POST    /join-a-tour/:id            controllers.Application.joinATourSubmit()

# Map static resources from the /public folder to the /assets URL path
GET     /assets/*file               controllers.Assets.at(path="/public", file)
